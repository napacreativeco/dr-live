{% comment %} 
*
* Artist Radios 
*
{% endcomment %}

<fieldset class="artist-radios">
    <label value="{{ 'Dais Records' }}">
        <input class="artist-radio" data-artist="{{ 'Dais Records' }}" type="radio" value="{{ 'Dais Records' }}">
        {{ 'Dais Records' }}
    </label>

    {% for coll in collections %}
        {% case coll.template_suffix %}
            {% when 'artist' %}

              
                {% for prod in coll.products %}
                    {% if prod.available == true %}
                        {% assign stock = true %}
                    {% else %}
                        {% assign stock = false %}
                    {% endif %}
                {% endfor %}

                {% if stock == true %}
                    <label value="{{ coll.title }}">
                        <input class="artist-radio" data-artist="{{ coll.title | url_escape }}" type="radio" value="{{ collection.title }}">
                        {{ coll.title }}
                    </label>
                {% endif %}

        {% endcase %}
    {% endfor %}
    
    {% comment %}  
    {% for product in collections.all.products %}

        {% case product.type %}
            {% when 'Vinyl' %}

                {% for collection in collections %}
                    {% if collection.title == product.vendor %}
                        <label value="{{ collection.title }}">
                            <input class="artist-radio" data-artist="{{ collection.title }}" type="radio" value="{{ collection.title }}">
                            {{ collection.title }}
                        </label>
                    {% endif %}
                {% endfor %}

        {% endcase %}

    {% endfor %}
    {% endcomment %}


</fieldset>
        
<script>
(function($) {
    $(document).ready(function() {
        $(".artist-radio").click(function(){
            var val =  $(this).attr('data-artist');
            window.location="?filter.p.vendor=" + val.replace('.', '');
            //window.location="?filter.p.collection=" + val;
        });
    });
})(jQuery);
</script>